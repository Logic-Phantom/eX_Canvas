# 확인 필요 (eX-Canvas Web Prototyper)

검증한 것과 가정한 것을 구분해 적는다.

## 검증함
- `Prototyper.clx` · 모듈 10종: `ci-lib/clx/e6-compiler.jar` 컴파일 문제 0건.
- 팔레트→캔버스 드롭, 더블클릭 추가, 이동, 속성 반영, 미리보기: 브라우저(런타임 exbuilder/runtime)에서 동작 확인.
- 생성 CLX 3종(템플릿 일반 · 템플릿 팝업+셔틀+탭 · XY): 같은 컴파일러로 컴파일 문제 0건, 실행 화면 구조 확인.
- Gemini: 서버 프록시 경로로 실호출 1회 성공(`gemini-2.5-flash`, responseSchema 통과).

## 확인 필요
0. **result 저장 · 컨트롤 확대 · UDC(2차 작업)** — 새 유형 16종 + UDC 4종을 XY·템플릿 두 방식으로 내보내 컴파일 문제 0건, `clx-src/result/20260920/` 저장 동작 확인(검증 파일은 삭제함). UDC 는 이 프로젝트에 eXCFrame 공통 모듈이 없어 캔버스에서 내용이 비어 보일 수 있다(이름표로 구분).
0-1. **저장 경로 3단(3차 작업)** — 저장 서버(DevServer·Tomcat) → 지정한 폴더(File System Access API) → 브라우저 다운로드. DevServer 경로는 브라우저에서 클릭까지 실제 저장 확인, 폴더 저장은 `saveToDirectory()` 를 모의 핸들로 검증(날짜 폴더 생성 · `.clx`/`.js` 2개 · `_HHmmss` 중복 회피). **아직 확인 못 한 것** : ① 실제 스튜디오/Tomcat 에서 `clx-src` 자동 탐색이 맞는 경로를 고르는지(이 PC 에서 톰캣을 띄우지 못했다) ② 실제 폴더 선택 창에서 권한 허용 후 파일이 만들어지는지(OS 대화상자라 자동화 불가).
0-2. **UI 템플릿 · 카탈로그 · 미리 배치(4차 작업)** — 상용구 116종을 카탈로그로 옮겨 팔레트에 올렸다.
   검증함 : 116종 전부 `templateBuilder.build()` 오류 0건 · 한 화면에 116종을 모두 담아 내보낸 `.clx` 컴파일 오류 0건 ·
   `[폼] 조회 (1행)` 브라우저 표시(사내 테마 그대로) · 패턴 21개 `skeleton→planByRule→resolve` 왕복 · 팔레트 찾기 상자 ·
   미리 배치가 캔버스를 꽉 채우는지(900×580 · 1600×900 에서 21개 모두 사방 여백 20px, 넘침 0).
   **아직 확인 못 한 것** : ① 116종을 하나씩 스튜디오 디자인 편집기에서 열어 본 것은 아니다(컴파일만 통과) ②
   `datatype`·`displayexp` 가 붙은 템플릿은 데이터셋이 없으면 런타임 경고가 날 수 있다 ③ 탭 아이콘 템플릿은
   `userAttributes`+표현식 바인딩을 옮기지 않아 아이콘이 빠진다.
0-4. **공유 · CRDT(5차 작업)** — 툴바 [공유] 체크박스로 켜고 끄는 실시간 협업.
   검증함(브라우저 탭 2개 ↔ 개발 서버 릴레이 `ws://127.0.0.1:8091`, 실제 웹소켓·실제 Yjs) :
   방 열기 → 나중 사람이 기존 항목 그대로 받기 → 추가 · **드래그 이동**(마우스 이벤트로 실제 드래그) · 크기 · `id`/`Text` 변경 ·
   삭제가 양쪽 같은 값 · 남의 커서(캔버스 좌표)와 선택 상자 추종 · 사람별 색 · 이름 변경 즉시 반영 ·
   공유 끄면 상대 화면에서 접속자/표시 즉시 정리 · 껐다 켜도 내 항목 유지 · 마지막 사람이 나가면 방 폐기(서버 로그).
   고친 것 : ① `userAttr()` 은 없는 속성에 `""` 를 준다(널 아님) → 접속자 표시용 컨트롤이 "항목" 으로 세어졌다. `attrOf()` 로 통일.
   ② 공유본이 onSynced 보다 먼저 도착하므로 "내 캔버스 vs 공유본" 판단은 **켜기 직전 uid 목록**으로 해야 한다.
   ③ awareness 는 릴레이가 모아 두지 않는다 → 소켓이 열리면 내 상태를 즉시 보내고, `added` 가 있으면 내 clientID 도 함께 실어 보낸다.
   ④ `clientID % 8` 은 값이 몰린다(관측값 2개 모두 0) → `% 1000003 % 8`.
   2026-09-22 macOS : **Tomcat 9.0.122(이클립스 배포, `localhost:8080`)에서 `CrdtRelayEndpoint` 로 탭 2개 항목·커서 동기화 확인.** Safari 18.6 은 esm.sh 동적 import · 웹소켓 접속 · 문서/awareness 전송까지 확인(자동 테스트 페이지).
   사용자가 "안 된다" 고 본 상황은 Chrome 이 Tomcat(`:8080`), Safari 가 개발 서버(`:8090`) 처럼 **서로 다른 릴레이**에 붙었거나, 이클립스 자동 재배포로 접속이 끊긴 경우로 보인다(접속 로그상 두 브라우저가 동시에 붙은 적 없음).
   **아직 확인 못 한 것** : ① Tomcat 10+ 는 `javax.websocket` → `jakarta.websocket` 으로 바꿔야 한다(미확인). ② 3명 이상 · 다른 PC 간 접속 ③ 느린 망에서의 재접속(3회 재시도) 동작
   ④ 오래 켜 둔 방의 기록 한도(16MB) 도달 시 거동 ⑤ 두 사람이 같은 항목을 동시에 드래그할 때의 체감(값은 나중 것이 이긴다).
0-5. **이미지로 배치 · 버튼 Style(6차 작업, 2026-09-22)** — 이미지 드롭/붙여넣기/파일 선택 → `imagePlanner`(Gemini 비전) → 캔버스 배치 + 패턴 콤보.
   검증함(개발 서버 · 실제 브라우저 이벤트) : 파일 드롭 · Ctrl+V · 캔버스 밖 드롭 무시 · 이미지 축소/base64 · Gemini 요청 본문(inline_data · 응답 스키마 · 유형 29종) ·
   **가짜 Gemini 응답(XHR 가로채기)** 으로 요소 27개 배치 · 필수 라벨 `*` · 버튼 primary/secondary 표시 · 패턴 콤보 설정(미리 배치 안 돌음) ·
   기존 항목 있을 때 confirm 뒤 교체(누적 안 됨) · 속성창 Style 변경 → 캔버스 클래스 + CLX `btn-primary-01` · 좌표 규칙이 같은 패턴(P3-1 · P2-4)으로 읽음 · `label required` 출력 · 컴파일 문제 0건.
   **아직 확인 못 한 것** : ① **실제 Gemini 응답** — 이 PC 에 API 키가 없어 실호출을 못 했다. 실제 캡처 몇 장으로 사각형·유형 정확도를 보고 `SYSTEM_TEXT` 를 다듬을 것(특히 표를 셀로 쪼개거나 라벨·입력을 한 상자로 합치는 경향).
   ② `inline_data`/`mime_type`(snake_case) 필드명은 REST 문서의 curl 예시를 따랐다(camelCase 도 받는다고 알려져 있음). ③ 긴 세로 캡처(스크롤 페이지)는 캔버스 아래로 넘쳐 스크롤된다.
   ④ 손그림·저해상도 이미지의 인식률. ⑤ Tomcat 프록시 8MB 상한은 컴파일만 확인(이 PC 에 스프링 클래스패스 없음).
   **서버 업로드 경로(같은 날 추가, eXConverter-AI 방식)** — 검증함 : 가짜 Gemini 서버(`-Dexcanvas.gemini.url`)를 두고 `curl -F image=@…` · 브라우저 드롭으로
   multipart 해석 → `CanvasImageAnalyzer` → 키 헤더 · inline_data · catalog/types 전달 → thinkingLevel 400 뒤 자동 재요청 → 코드 펜스 벗김 → 16개 배치 · P3-2.
   헤더 없는 POST 403 · `imageStatus.do` · 키 없을 때 503 문구 · 직접 호출 설정이어도 키가 비면 서버로 가는 것 확인. `CanvasImageController` 는 WEB-INF/lib + Tomcat servlet-api 로 javac 통과.
   **아직 확인 못 한 것** : ⑥ 실제 Tomcat 배포에서 `multipartResolver` 빈이 다른 컨트롤러(`DataRequestResolver`)와 충돌하지 않는지 ⑦ 실제 Gemini 응답 ⑧ 이클립스가 `src/main/resources/canvas/*` 를 WEB-INF/classes 로 잘 배포하는지(매핑은 있음).
0-6. **API 연동 · Swagger/OpenAPI(7차 작업, 2026-09-23)** — 명세 JSON → DataSet · DataMap · Submission → 바인딩된 뼈대 → `.clx` + `.js` 핸들러.
   검증함(개발 서버 · 내장 브라우저 · 샘플 `tools/harness/sample-openapi.json` 을 다른 포트 18437 에 두고) : Swagger UI 주소 → 문서 주소 추정 · 프록시 수신 ·
   역할 판정(목록/등록/상세/수정/삭제 · POST 조회 · 중첩 `data.content` · 자식 배열은 목록 아님) · `dmDetail` 공유 · [화면 생성] 41개 항목 + 패턴 P3-1(좌표 규칙도 P3-1) ·
   [미리보기]/[result 저장] 의 `<cl:model>`(1/4/5) · `datamapbind` 16 · 그리드 `datasetid`+`columnname` · 리스너 6 → `.js` 핸들러 6 · 저장 파일 `e6-compiler` 컴파일 오류 0건 ·
   XY 모드 동일 · Swagger 2.0 매핑 · 속성창 Bind 표시 · 프록시 403/400/404.
   고친 것 : ① 응답 객체의 자식 배열(`careers`)을 목록으로 오판 → 래퍼 판정(`listProperty`)으로 좁힘 ② 폼이 길면 하단 버튼과 겹침 → 버튼을 폼 아래로 ③ `requestdata`/`responsedata`/`datamapbind` 의 `std:sid` → 스키마상 없음, 뗌 ④ 콤보·목록 상자 비우기는 `deleteAllItems()`(`removeAllItems` 없음).
   **아직 확인 못 한 것** : ① 실제 API 서버로 `send()` 실행(응답 alias `data.content` 같은 점 경로를 런타임이 받는지 — 못 받으면 `submit-done` 핸들러로 바꿀 것) ② 스튜디오 디자인 편집기에서 생성 `.clx` 의 모델·바인딩 표시 ③ Tomcat 의 `CanvasOpenApiController` — 9/24 실 Tomcat 에서 `probe=1` 200 까지 확인, 명세 수신은 미확인 ④ 사설 인증서 https 명세 ⑤ YAML 명세(지원 안 함 · 안내만) ⑥ 아주 큰 명세(수백 API)의 목록 상자 사용성.
0-7. **Tomcat 배포본 오류 조치(2026-09-24)** — 이클립스 Tomcat(`localhost:8080`)에서 화면이 뜰 때 `Cannot read properties of undefined (reading 'probeProxy')` + `GET /canvas/collabInfo.do 404`.
   원인 : 9/23 에 밖에서 만든 `openApiPlanner.module.js` · `CanvasOpenApiController.java` 를 이클립스가 새로 고침 전이라 몰라 `clx-build/cpr-lib/user-modules.js`(9/22 · 모듈 11종)와 `target/classes`(컨트롤러 없음)가 그대로 배포됨.
   `Prototyper.clx.js` 만 9/24 11:06 에 다시 빌드돼(편집기에서 연 파일만 갱신) **새 화면 코드가 옛 번들을 불러** `require()` 가 undefined. `collabInfo.do` 는 개발 서버에만 있던 엔드포인트라 Tomcat 에서는 원래 404(같은 포트로 대체돼 동작은 됐지만 콘솔에 남음).
   조치 : ① `mod()` 가 빠진 모듈 이름·조치를 오류로 알림 + `initApiPanel()` 은 모듈이 없어도 초기화 완료(API 버튼만 꺼짐) ② Tomcat 용 `CanvasCollabController`(`collabInfo.do` → `{ok, path, rooms}`, 개발 서버와 같은 계약 · `CrdtRelayEndpoint.WS_PATH`/`roomCount()`)
   ③ CLI(`BuildOnce`) 산출물 `user-modules.js` · `Prototyper.clx.js` · `prototyper.css` 와 `javac --release 8`(WEB-INF/lib + Tomcat servlet/websocket-api) 클래스를 `clx-build` · `target/classes` · wtpwebapps 배포본에 복사(`env.json` 은 이클립스 것이 항목이 더 많아 복사하지 않음).
   검증함 : Tomcat 이 `reloadable` 로 다시 읽은 뒤 `collabInfo.do` 200 `{"path":"/ws/crdt-sync.do","rooms":0,"ok":true}` · `fetchOpenApi.do?probe=1` 200 · 배포 번들 모듈 12종 · 내장 브라우저로 화면을 열어 콘솔 오류 0(런타임 버전 로그만) · 뜰 때의 요청 5개(saveResult · collabInfo · imageStatus · fetchOpenApi probe) 모두 200 · API 연동 안내가 "서버 프록시가 대신 받습니다" 로 표시.
   이클립스 컴파일러(6190)와 CLI 산출물의 차이는 헤더 · 미디어쿼리 소수점(`1439.984px`/`1439px`) · 문장 순서뿐(의미 같음).
   **아직 확인 못 한 것** : ① 이클립스 F5 → 빌드 → Publish 뒤 이클립스 산출물로도 같은 결과인지(덮어써도 문제없을 것으로 보지만 실행은 안 해 봄) ② 재발 방지로 Preferences → General → Workspace → "Refresh using native hooks or polling" 을 켜면 밖에서 만든 새 파일도 자동으로 빌드되는지.
0-8. **응답 샘플 JSON → DataSet/DataMap(2026-09-24)** — 명세 대신 실제 응답 JSON(목록형 `{dsList:[…], dmPageInfo:{…}}` · 계층형)을 넣으면 배열 키 = DataSet, 객체 키 = DataMap, 중첩은 재귀로 자식 항목.
   **쓰임새는 "화면 생성" 이 아니라 "이 화면의 모델"** 이다 — 분석 즉시 `moApiModel` 로 붙고(`applySampleModel` · 목록 상자 selection-change 로 다시 붙음), 미리 배치(템플릿)든 팔레트든 이미지든 화면을 만든 뒤 내보내면 `<cl:model>` 에 들어간다. 그리드는 `addCanvasItem()` 에서 아직 안 쓴 첫 DataSet 에 `ds:` 로 저절로 이어지고(`nextDataSetBind`), 먼저 그린 그리드는 분석 시점에 `autoBindGrids()` 가 잇는다. 모델은 [전체 삭제] 때만 떨어진다(`clearCanvas()` 는 건드리지 않음). 응답 샘플에서는 [화면 생성]·[모델만 적용] 을 숨긴다(`grpApiActions.visible`) — 샘플용 뼈대 함수(`sampleSkeleton`)는 뺐다.
   검증함 : 붙여넣기 분석만 한 뒤 미리 배치 P1-1 → 뼈대 9개 중 `grd1 [ds:dsList]` · [미리보기] `<cl:model>` 에 `dsList` · `dmPageInfo` + `datasetid="dsList"` · 팔레트 Grid 더블클릭 → DataSet 이 남지 않아 `grd2 []` · 분석 뒤 `grpApiActions.visible == false`(버튼 숨김) · 목록 상자에서 `dmPageInfo` 만 클릭 → 상태 "DataSet 0 · DataMap 1"(selection-change 로 즉시 갱신).
   참고 : 자동 시험에서 `cbxPrefill`·`cmbPattern` 의 `value` 를 코드로 바꾸면 value-change/selection-change 가 나지 않아 미리 배치가 돌지 않는다 — 체크박스를 실제로 껐다 켜야 한다(사람 조작은 문제없음).
   검증함(실 Tomcat `:8080` · 내장 브라우저 · `tools/harness/sample-response-*.json`) : 목록형 → `dsList`(29 컬럼 · number 자료형) + `dmPageInfo`(페이지 정보 → 모델만) · 화면 생성 P1-1 · `<cl:model>` + 그리드 바인딩 58 ·
   계층형 → 항목 7개(`dmCustomerInfo` · `dmContact` · `dsAccounts` · `dsTransactions`(두 계좌 6행 이어 붙임) · `dmCreditInfo` · `dsPaymentHistory` · `dmResult`) · 화면 생성 13개(P3-1) · 같은 컬럼이 없는 그리드↔폼은 selection-change 를 만들지 않음(리스너 0) · 저장 파일 `e6-compiler` 오류 0건(삭제함).
   고친 것 : ① DOM `textarea.value` 를 바꿔도 cpr 컨트롤은 모른다 → 자동 시험은 `cpr.core.Platform.INSTANCE.getAllRunningAppInstances()` 로 앱을 찾아 `lookup("txaApiJson").value` 로 넣었다(사람이 붙여넣을 때는 문제없음). ② 처음엔 계층형에서 무관한 그리드(계좌)→폼(고객) 복사 핸들러가 생겨 같은 이름의 컬럼이 있을 때만 `flows.form` 을 두도록 고쳤다.
   **아직 확인 못 한 것** : ① 자식 DataSet(`dsTransactions`)이 어느 상위 행의 것인지는 상위 키 컬럼이 행에 있을 때만 구분된다 — 마스터-디테일 필터/관계는 만들지 않는다 ② Submission 이 없으므로 스튜디오에서 잇는 절차(alias 를 JSON 키로 · 중첩 경로는 submit-done)는 실제 런타임으로 확인하지 않았다 ③ 페이지 정보 판정(`INFO_COLUMN` 60%)이 실제 사내 응답들(예: `dmPageInfo` 외의 이름)에서도 맞는지 ④ 3개 이상의 그리드를 위아래로 쌓으면 캔버스가 좁아 스크롤된다(선택해서 [화면 생성] 하는 것으로 충분한지).
0-3. **CLI 컴파일러 한계(중요)** — `e6-compiler.jar` 로 `clx-src/theme` 를 포함해 빌드하면 `custom-theme.less` 단계에서
   끝나지 않는다(CPU 를 계속 쓰며 BUILD SUCCESS 가 나오지 않음, 40분 확인). 같은 LESS 를 이클립스의 eXBuilder6
   빌더는 몇 초에 컴파일한다. 그래서 `tools/BuildOnce.java` 가 `--exclude theme/**` 로 빌드하고 `clx-build/theme` 를
   덮어 넣는다. 원인은 못 찾았다(힙 증설·`--relativize-less-url`·출력 폴더 선 배치 모두 효과 없음).
   프로토타이퍼 전용 스타일은 `clx-src/theme/custom/prototyper.less` → `clx-src/style/prototyper.less` 로 옮겨
   테마와 무관하게 CLI 가 컴파일하도록 했다(`env.json` 의 runtime-css 도 함께 고침).
1. **스튜디오 디자인 편집기 표시** — 컴파일러는 통과했지만 스키마 검증은 스튜디오만 한다. 생성한 `.clx` 와 `Prototyper.clx` 를 스튜디오에서 열어 확인할 것.
   (스킬의 `check_clx.py --xsd` 는 이 PC 에 Python 이 없어 돌리지 못했다.)
2. **스크린 정의** — `Prototyper.clx` 는 사업부 기본 L·M·S, 생성 CLX 는 템플릿과 같은 `EXB-FULL/DIV/PART/POP`. 대상 프로젝트의 스크린 id 가 다르면 `clxSerializer.headEl()` 을 맞출 것.
3. **타이틀 UDC 속성 전달** — 템플릿은 UDC 에 속성을 주지 않는다. 생성물은 제목이 있을 때만 `<cl:property name="title" …/>` 를 넣는다(UDC appspec 에 `title` 존재 확인함). `ctrl`(그리드 연결) 속성은 넣지 않았다.
4. **그리드 헤더 `text`** — 템플릿의 그리드 셀은 비어 있다. 생성물은 헤더 `text` 를 채운다(컴파일 통과). `columnname`/데이터셋은 만들지 않는다.
5. **Gemini 모델명** — 기본값 `gemini-2.5-flash`. 무료 등급에서 쓸 수 있는 모델·한도는 Google 정책에 따라 바뀌므로 AI Studio 에서 확인.
6. **연속 드래그** — 자동화 도구로 빠르게 3회 연속 드래그하면 3번째가 누락되는 현상이 있었다(단독 재시도는 성공). 사람 손 조작에서 재현되는지 확인.
7. **프로젝트 의존성(기존 문제)** — `udc/com/*` 가 `createCommonUtil`(eXCFrame 공통 모듈)을 요구하는데 이 프로젝트에는 그 모듈이 없다. 생성 CLX 를 이 프로젝트에서 실행하면 콘솔 오류가 난다. eXCFrame 템플릿 프로젝트에서 열 것.
